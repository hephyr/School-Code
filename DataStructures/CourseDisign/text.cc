#include <iostream>
#include <vector>
#include <regex>
#include <fstream>
#include <string>
#include "school.h"
using std::cout; using std::endl;
using std::vector; using std::string;
using std::regex;
int main() {
	int i = 1;
	vector<int> v;
	v.push_back(i);
	i = 2;
	cout << v[0] << endl;
    return 0;
}